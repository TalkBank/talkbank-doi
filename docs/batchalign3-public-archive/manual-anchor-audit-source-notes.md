# Manual Anchor Audit Source Notes

This archive note preserves the run-specific local-source details removed from
the public Batchalign3 manual-anchor page during public-release cleanup.

## Public page

- `batchalign3/book/src/developer/manual-anchor-audit.md`

## Local/source-specific details intentionally kept private

- exact local checkout path used for the audit run
- exact local HTML export paths for CHAT and CLAN manuals
- exact local DOCX source paths
- machine-readable JSON outputs tied to that local run environment

## Why this note exists

The public book should explain the maintenance contract and the current result,
not depend on maintainer-local filesystem layout. The raw run metadata remains
useful for maintainers reproducing or comparing audits.
