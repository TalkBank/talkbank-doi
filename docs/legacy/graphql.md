# GraphQL

[GraphQL](https://graphql.org/) is "a query language for APIs and a
runtime for fulfilling those queries with your existing data".

TODO Franklin only started evaluating GraphQL for exposing queries on
CHAT data, as an alternative to simply providing an API.
