# Fleet History

Archived from the public `batchalign3` book on 2026-03-05.
Fleet fan-out and related multi-server planning did not make the first public
release surface. Historical notes and rewrite-era context live here instead.

## Historical Fleet Features

The earlier architecture docs described capacity-weighted multi-server fan-out,
fleet-wide CLI views, and richer fleet dashboard behavior.

That design intent is still visible in retained helper/modules and comments, but
it is not currently enabled in the active CLI dispatch path. Keep any planning
or proposal work clearly labeled as historical or future-state until fan-out is
re-enabled.
